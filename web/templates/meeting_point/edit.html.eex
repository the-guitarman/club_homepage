<h2><%= gettext("edit_meeting_point") %></h2>

<%= render "form.html", changeset: @changeset,
                        action: meeting_point_path(@conn, :update, @meeting_point) %>

<%= link gettext("all") <> " " <> gettext("meeting_points"), to: meeting_point_path(@conn, :index) %>
