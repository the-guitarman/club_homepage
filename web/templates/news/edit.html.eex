<div class="row">
    <div class="col-xs-12 col-sm-8 col-sm-offset-2">
        <div class="jumbotron content-box">
            <h2><%= gettext("edit_news") %></h2>

            <%= render "form.html", changeset: @changeset,
                        action: news_path(@conn, :update, @news) %>

            <%= link gettext("all") <> " " <> gettext("news"), to: news_path(@conn, :index) %>
        </div>
    </div>
</div>
