<div class="row">
  <div class="col-xs-12 col-sm-8 col-sm-offset-2">
    <div class="jumbotron content-box">
      <h2><%= gettext("edit_beer_list") %></h2>

      <%= render "form.html", changeset: @changeset,
                              action: beer_list_path(@conn, :update, @beer_list) %>

      <%= link gettext("all") <> " " <> gettext("beer_lists"), to: beer_list_path(@conn, :index) %>
    </div>
  </div>
</div>
