<h2>Edit beer list</h2>

<%= render "form.html", changeset: @changeset,
                        action: beer_list_path(@conn, :update, @beer_list) %>

<%= link "Back", to: beer_list_path(@conn, :index) %>
