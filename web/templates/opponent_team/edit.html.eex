<h2><%= gettext("edit_opponent_team") %></h2>

<%= render "form.html", changeset: @changeset,
                        action: opponent_team_path(@conn, :update, @opponent_team) %>

<%= link gettext("all") <> " " <> gettext("opponent_teams"), to: opponent_team_path(@conn, :index) %>
