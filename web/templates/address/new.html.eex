<h2><%= gettext("create_new_address") %></h2>

<%= render "form.html", changeset: @changeset,
                        action: address_path(@conn, :create) %>

<%= link gettext("all") <> " " <> gettext("addresses"), to: address_path(@conn, :index) %>
