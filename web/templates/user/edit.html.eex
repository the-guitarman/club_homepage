<h2><%= gettext("edit_user") %></h2>

<%= render "form.html", changeset: @changeset,
                        action: user_path(@conn, :update, @user) %>

<%= link gettext("all") <> " " <> gettext("users"), to: user_path(@conn, :index) %>
