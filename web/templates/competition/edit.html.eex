<h2>Edit competition</h2>

<%= render "form.html", changeset: @changeset,
                        action: competition_path(@conn, :update, @competition) %>

<%= link "Back", to: competition_path(@conn, :index) %>
